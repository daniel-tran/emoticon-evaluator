Modularise the code so the data-related functions are in their own source files

Simplify implementation by using a "pseudo-regex" approach

Implement project using proper regex (low priority)

Include non-ASCII emoticons such as ಠ_ಠ (low priority)
